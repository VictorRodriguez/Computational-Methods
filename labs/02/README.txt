Diego Sebastian Garcia Cabrera A01634071

Se importa la biblioteca Graphviz que permite la visualización de gráficos.
Se define la clase RegularExpression que representa una expresión regular.
Se define la clase State que representa un estado del autómata.
Se define la función regex_to_nfa que convierte una expresión regular en un autómata finito no determinista (NFA).
Se define la función nfa_to_dot que convierte un NFA en una representación de Graphviz.
Se prueba el programa con una expresión regular y se guarda la representación en un archivo de registro (log_file).
